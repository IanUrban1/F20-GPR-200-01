#version 300 es 


#ifdef GL_ES
precision highp float;
#endif

layout (location = 0) out vec4 rtFragColor;
//out vec4 rtFragColor;


//varying
//ps is always in, vs is al	ways out	

//PER-VERTEX
in vec4 vColor;


//PER-FRAGMENT: takes in requirements for final color
in vec4 vNormal;
in vec4 vTexCoord;
in vec4 vPosition;


void main()
{
	//output vertex lights
	//rtFragColor = vColor;
	
	//output for fragment lights
	//rtFragColor = vNormal
	//rtFragColor = vPosition;
	
	
	//PER-FRAGMENT: Inputs used to calculate final color
	//stops streching
	vec4 N = normalize(vNormal);
	
	rtFragColor = vec4(N.xyz * 0.5 + 0.5, 1.0);
	
	//rtFragColor = vTexCoord;	
}
